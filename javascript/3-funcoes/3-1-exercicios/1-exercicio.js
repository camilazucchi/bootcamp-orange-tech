function escrevaMeuNome(nome) {
    console.log('meu nome é ' + nome);
}

escrevaMeuNome('camila');

function isMaiorDeIdade (idade) {
    if (idade >= 18) {
        console.log(`é maior de idade.`)
    } else {
        console.log(`não é maior de idade!`)
    }
}

isMaiorDeIdade(18);